import { assert } from "console"
import { hello } from "../src/expense/functions/handler"

describe('Example Test', () => {
    it('should return a success response', () => {
      expect(1 + 1).toBe(2);
    });
});
  